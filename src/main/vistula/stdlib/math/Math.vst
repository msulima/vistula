
def sinDeg(angle: vistula.lang.Integer) {
   '''# javascript
   return Math.sin(angle * Math.PI / 180);
   '''
   0
}

def cosDeg(angle: vistula.lang.Integer) {
   '''# javascript
   return Math.cos(angle * Math.PI / 180);
   '''
   0
}
